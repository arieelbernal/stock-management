server:
  port: 8080

spring:
  application:
    name: config-service
  cloud:
    config:
      server:
        git:
          uri: ssh://git@github.com:arieelbernal/stock-management.git
          searchPaths: config-data
          clone-on-start: true
          private-key: $${SSH_PRIVATE_KEY}
          ignore-local-ssh-settings: true  # Opcional, para ignorar las configuraciones locales de SSH
          default-label: develop
  security:
    user:
      name: root
      password: s3cr37
  profiles:
    active: dev

---

spring:
  profiles: dev
  cloud:
    config:
      profile: dev