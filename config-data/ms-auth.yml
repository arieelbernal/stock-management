server:
  port: 8081

spring:
  application:
    name: ms-auth
  datasource:
    url: jdbc:mysql://localhost:3306/auth_test?useSSL=false&serverTimezone=UTC
    username: db-connector
    password: 654321test?
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

springdoc:
  api-docs:
    path: /api-docs

eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8099/eureka